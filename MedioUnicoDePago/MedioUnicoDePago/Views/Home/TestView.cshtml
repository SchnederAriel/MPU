<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Listado de Medios de Pago Disponibles - ANSES (Simulado)</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        /* Página principal */
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f6f7;
            margin: 0 auto;
            padding: 40px;
            max-width: 720px;
            color: #212529;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        h1 {
            color: #0056b3;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #6c757d;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .alert-info {
            background-color: #f1f9fc;
            border: 1px solid #7bc0df;
            color: #0c5460;
            font-size: 14px;
            border-radius: 4px;
            padding: 12px 15px;
            margin-bottom: 25px;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            color: white;
            padding: 8px 16px;
            font-size: 14px;
            border-radius: 4px;
            cursor: pointer;
        }

            .btn-primary:hover {
                background-color: #0056b3;
                border-color: #0056b3;
            }

        /* Confirm box personalizado */
        .confirm-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

            .confirm-overlay.visible {
                display: flex;
                opacity: 1;
            }

        .confirm-box {
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
            padding: 24px;
            width: 90%;
            max-width: 400px;
            text-align: center;
            transform: scale(0.9);
            transition: transform 0.3s ease;
        }

        .confirm-overlay.visible .confirm-box {
            transform: scale(1);
        }

        .confirm-title {
            margin: 0 0 12px;
            font-size: 1.5rem;
            color: #333;
        }

        .confirm-message {
            margin: 0 0 24px;
            font-size: 1rem;
            color: #666;
            line-height: 1.5;
        }

        .confirm-buttons {
            display: flex;
            justify-content: center;
            gap: 16px;
        }

        .confirm-button {
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s ease, transform 0.1s ease;
            flex-grow: 1;
        }

            .confirm-button:active {
                transform: translateY(1px);
            }

        .confirm-button-ok {
            background-color: #007bff;
            color: white;
        }

            .confirm-button-ok:hover {
                background-color: #0056b3;
            }

        .confirm-button-cancel {
            background-color: #e9ecef;
            color: #495057;
        }

            .confirm-button-cancel:hover {
                background-color: #ced4da;
            }
    </style>
</head>
<body>
    <div class="container">
        <h1>Seleccioná tu medio de cobro para recibir tus asignaciones</h1>
        <hr />
        <p class="subtitle">
            Simulación: tenés dos medios de pago activos. Al cambiar, reemplazás permanentemente el medio actual.
        </p>
        <div class="alert alert-info" role="alert">
            <strong>Importante:</strong> al seleccionar un nuevo medio de cobro reemplazás el medio actual.
        </div>

        <!-- Simulación de Model.Count > 0 -->
        <form id="formMedioPago">
            <div class="mb-4">
                <h5 class="mb-3">Medios de pago disponibles</h5>

                <!-- Medio de pago 1: CBU -->
                <div class="form-check">
                    <input class="form-check-input" type="radio"
                           name="medioPagoSeleccionado"
                           id="CBU-000111222333"
                           value="CBU"
                           data-cbu="000111222333">
                    <label class="form-check-label" for="CBU-000111222333">
                        Banco Simulado - CBU: 000111222333
                    </label>
                </div>

                <!-- Medio de pago 2: Banco físico -->
                <div class="form-check">
                    <input class="form-check-input" type="radio"
                           name="medioPagoSeleccionado"
                           id="Banco-123-456"
                           value="Banco"
                           data-codigo-banco="123"
                           data-codigo-agencia="456">
                    <label class="form-check-label" for="Banco-123-456">
                        Banco Ejemplo - Agencia 456 (Código Banco: 123)
                    </label>
                </div>

                <!-- Opción de nuevo medio -->
                <div class="form-check">
                    <input class="form-check-input" type="radio"
                           name="medioPagoSeleccionado"
                           id="ninguno"
                           value="ninguno">
                    <label class="form-check-label" for="ninguno">
                        Nuevo Medio de Cobro
                    </label>
                </div>
            </div>
        </form>

        <div class="d-flex justify-content-start mt-4">
            <button type="button" id="btnContinuar" class="btn btn-primary">Continuar</button>
        </div>
    </div>

    <!-- Confirm Box -->
    <div id="custom-confirm" class="confirm-overlay">
        <div class="confirm-box">
            <h2 id="confirm-title" class="confirm-title">Confirmar Cambio</h2>
            <p id="confirm-message" class="confirm-message">
                Al cambiar el medio de cobro, reemplazás permanentemente el medio actual. ¿Querés continuar?
            </p>
            <div class="confirm-buttons">
                <button id="confirm-cancel" class="confirm-button confirm-button-cancel">Cancelar</button>
                <button id="confirm-ok" class="confirm-button confirm-button-ok">Aceptar</button>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // CustomConfirm class
        class CustomConfirm {
            constructor() { this.resolve = null; }
            show({ title, message, okText = 'Aceptar', cancelText = 'Cancelar' }) {
                $('#confirm-title').text(title);
                $('#confirm-message').text(message);
                $('#confirm-ok').text(okText);
                $('#confirm-cancel').text(cancelText);
                $('#custom-confirm').addClass('visible');
                return new Promise(res => { this.resolve = res; });
            }
            hide() { $('#custom-confirm').removeClass('visible'); }
            handleOk() { if (this.resolve) this.resolve(true); this.hide(); }
            handleCancel() { if (this.resolve) this.resolve(false); this.hide(); }
        }

        $(document).ready(function () {
            const myConfirm = new CustomConfirm();
            $('#confirm-ok').on('click', () => myConfirm.handleOk());
            $('#confirm-cancel').on('click', () => myConfirm.handleCancel());

            $('#btnContinuar').click(async function (e) {
                e.preventDefault();
                const seleccion = $('input[name="medioPagoSeleccionado"]:checked');
                if (seleccion.length === 0) {
                    alert('Por favor, seleccione un medio de pago.');
                    return;
                }

                // Simulamos Model.Count > 0 siempre true
                if (seleccion.val() !== 'ninguno') {
                    const confirmed = await myConfirm.show({
                        title: '¿Estás seguro?',
                        message: 'Al cambiar el medio de cobro, reemplazás permanentemente el medio actual. ¿Querés continuar?',
                        okText: 'Sí, continuar',
                        cancelText: 'No, cancelar'
                    });
                    if (!confirmed) return;
                }

                if (seleccion.val() === 'ninguno') {
                    alert('Iría a la página de Nuevo Medio de Cobro...');
                } else {
                    // Aquí normalmente harías el AJAX; para simulación:
                    alert('Guardando: ' + seleccion.val());
                }
            });
        });
    </script>
</body>
</html>
