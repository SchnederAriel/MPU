
@{
    ViewBag.Title = "Medio de Pago Único - ANSES";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/Site.css" rel="stylesheet" />
<style>
    body {
        background-color: #dcdcdc;
        color: #333;
        font-family: 'Arial', sans-serif;
    }

    .container {
        max-width: 800px;
        margin-top: 50px;
    }

    .card {
        border: none;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .card-body {
        padding: 30px;
    }

    h1 {
        color: #0056b3;
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
    }

    .subtitle {
        color: #6c757d;
        font-size: 16px;
        margin-bottom: 20px;
    }

    .alert-info {
        background-color: #e8f4f8;
        border-color: #d6e9f0;
        color: #0c5460;
        font-size: 14px;
        border-radius: 5px;
        padding: 15px;
        margin-bottom: 20px;
    }

    .section-title {
        font-weight: bold;
        margin-bottom: 10px;
        font-size: 18px;
    }

    .blue-bg {
        background-color: #007bff;
        color: white;
        padding: 10px;
        margin-bottom: 10px;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
</style>

<div class="container">
    <div class="card">
        <div class="card-body">
            <h1>Seleccione su Medio de Cobro para percibir todas sus prestaciones</h1>

            <div class="mb-4">
                <!-- AS - 20250514 - MEJORA A6) Debe decir: Para cobrar tus asignaciones familiares  y otras prestaciones (Asignacion Universal, Voucher educativo, etc. ) podés elegir entre: acreditación en cuenta (CBU), banco digital o billetera virtual, o banco por código postal (si elegís esta opción, al ir cobrar por primera vez, tenés que firmar la apertura de la cuenta).   -->
                <!-- <h5 class="section-title">Cambio de lugar de cobro.</h5> -->
                <p class="subtitle">Para cobrar tus asignaciones familiares  y otras prestaciones (Asignacion Universal, Voucher educativo, etc. ) podés elegir entre: acreditación en cuenta (CBU), banco digital o billetera virtual, o banco por código postal (si elegís esta opción, al ir cobrar por primera vez, tenés que firmar la apertura de la cuenta). </p>
            </div>

            <div class="alert alert-info" role="alert">
                <!-- AS - 20250514 - MEJORA A7) Sugiere eliminar "Forma de pago", Debe decir: al seleccionar un nuevo medio de cobro reemplazás en forma permanente el medio donde actualmente cobrás tus asignaciones familiares y otras prestaciones (Asignacion Universal, Voucher educativo, etc. ).   -->
                <strong>Importante:</strong> al seleccionar un nuevo medio de cobro reemplazás en forma permanente el medio donde actualmente cobrás tus asignaciones familiares y otras prestaciones (Asignacion Universal, Voucher educativo, etc. ).
            </div>

                <div class="mb-4">
                    <!-- AS - 20250514 - MEJORA A22) Debe decir: Medio de cobro actual -->
                    <h5 class="section-title">Medio de cobro actual</h5>
                    <div class="blue-bg">
                        <!-- AS - 20250514 - MEJORA A23) Debe decir: Medio de cobro -->
                        <strong>Medio de cobro</strong>
                    </div>
                    <p class="mt-2">@(ViewBag.MedioPagoActual != null && !string.IsNullOrEmpty(ViewBag.MedioPagoActual) ? ViewBag.MedioPagoActual : "No tiene MPU ")</p>
                </div>

                <div class="d-flex justify-content-end mt-4">
                    @if (ViewBag.UDAI != null && (bool)ViewBag.UDAI)
                    {
                        <a href="@Url.Action("GuardarMDP", "Home")" class="btn btn-primary">Modificar</a> }
                    else
                    {
                        if ((bool)ViewBag.NoModificar==false)
                        {
                            if (ViewBag.ValidarPersona != null && (bool)ViewBag.ValidarPersona)
                            {
                                <a href="@Url.Action("VerificarCelular", "Home")" class="btn btn-primary">Modificar</a>


                            }
                            else
                            {
                                <a href="@Url.Action("GuardarMDP", "Home")" class="btn btn-primary">Modificar</a>
                            }
                        }
                        else
                        {
                <p class="danger">
                    Es necesario que mantengas tu medio de cobro actual por al menos @ViewBag.DiasNoDejaCargarMDP días. Luego podrás realizar el cambio.
                </p>
                                            }

                                        }


                </div>


            </div>
        </div>
    </div>



