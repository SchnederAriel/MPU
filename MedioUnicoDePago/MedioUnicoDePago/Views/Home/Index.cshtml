
@{
    ViewBag.Title = "Medio de Pago Único - ANSES";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/Site.css" rel="stylesheet" />
<style>
    body {
        background-color: #dcdcdc;
        color: #333;
        font-family: 'Arial', sans-serif;
    }

    .container {
        max-width: 800px;
        margin-top: 50px;
    }

    .card {
        border: none;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .card-body {
        padding: 30px;
    }

    h1 {
        color: #0056b3;
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
    }

    .subtitle {
        color: #6c757d;
        font-size: 16px;
        margin-bottom: 20px;
    }

    .alert-info {
        background-color: #e8f4f8;
        border-color: #d6e9f0;
        color: #0c5460;
        font-size: 14px;
        border-radius: 5px;
        padding: 15px;
        margin-bottom: 20px;
    }

    .section-title {
        font-weight: bold;
        margin-bottom: 10px;
        font-size: 18px;
    }

    .blue-bg {
        background-color: #007bff;
        color: white;
        padding: 10px;
        margin-bottom: 10px;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
</style>

<div class="container">
    <div class="card">
        <div class="card-body">
            <h1>Seleccione su Medio de Cobro para percibir todas sus prestaciones</h1>

            <div class="mb-4">
                <h5 class="section-title">Cambio de lugar de cobro.</h5>
                <p class="subtitle">Si quiere cambiar el lugar donde recibe su asignación familiar podrá elegir entre: acreditación en cuenta (CBU), banco por código postal o billetera virtual. Simplifique su trámite según sus preferencias. ¡La elección es comodidad!</p>
            </div>

            <div class="alert alert-info" role="alert">
                <strong>Forma de pago - Importante:</strong> Al elegir un nuevo medio, éste sustituirá permanentemente la opción anterior.
            </div>

            <div class="mb-4">
                <h5 class="section-title">Medio de pago actual</h5>
                <div class="blue-bg">
                    <strong>Medio de pago</strong>
                </div>
                <p class="mt-2">@(ViewBag.MedioPagoActual != null && !string.IsNullOrEmpty(ViewBag.MedioPagoActual) ? ViewBag.MedioPagoActual : "No tiene MPU ")</p>
            </div>

            <div class="d-flex justify-content-end mt-4">
                @if (ViewBag.UDAI != null && (bool)ViewBag.UDAI)
                {
                    <a href="@Url.Action("GuardarMDP", "Home")" class="btn btn-primary">Modificar</a> }
                else
                {
                    if ((bool)ViewBag.NoModificar==false)
                    {
                        if (ViewBag.ValidarPersona != null && (bool)ViewBag.ValidarPersona)
                        {
                            <a href="@Url.Action("VerificarCelular", "Home")" class="btn btn-primary">Modificar</a>


                        }
                        else
                        {
                            <a href="@Url.Action("GuardarMDP", "Home")" class="btn btn-primary">Modificar</a>
                        }
                    }
                    else
                    {
            <p class="danger">
                Es necesario que mantengas tu medio de cobro actual por al menos @ViewBag.DiasNoDejaCargarMDP días. Luego podrás realizar el cambio.
            </p>
                                        }

                                    }


            </div>


        </div>
    </div>
</div>



